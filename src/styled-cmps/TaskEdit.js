import styled from 'styled-components';

export const StyledTaskEdit = styled.section`
      position: fixed;
      top: ${props => props.top}px;
      left: ${props => props.left}px;
      transform: translateX(-50%)
`